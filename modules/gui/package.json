{
  "name": "gui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "build-all": "npm run build && tauri build && npx cpy-cli src-tauri/target/release/gui bin --flat"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.2.0",
    "cpy-cli": "^4.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.2.2",
    "typescript": "^4.8.2",
    "vite": "^4.0.0"
  }
}
